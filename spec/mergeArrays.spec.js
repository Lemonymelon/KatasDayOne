const { describe, it } = require('mocha');
// const mergeArrays = require('../katas/mergeArrays');

describe('mergeArrays', () => {
  it('describe what happens if this test succeeds here', () => {
    // expect(mergeArrays(???)).to...
  });
});
