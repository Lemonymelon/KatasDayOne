// const {max, min} = require('../maxAndMin');

describe('max', () => {
  it ('describe what happens if this test succeeds here', () => {
    // expect(max(???)).to...
  });
});

describe('min', () => {
  it ('describe what happens if this test succeeds here', () => {
    // expect(min(???)).to...
  });
});